// Copyright (c) Microsoft Corporation. All rights reserved.
//
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::models::{
    NIClientGetStatusOptions, NIClientIncorrectCustomOpRefOperationStatus,
    NIClientIncorrectCustomOpRefOptions, NIClientPartialBodyOperationStatus,
    NIClientPartialBodyOptions, PartialBodyRequest, X,
};
use azure_core::{
    error::{CheckSuccessOptions, Error, ErrorKind},
    http::{
        headers::{HeaderName, RETRY_AFTER, RETRY_AFTER_MS, X_MS_RETRY_AFTER_MS},
        poller::{get_retry_after, PollerResult, PollerState, PollerStatus, StatusMonitor as _},
        Method, Pipeline, PipelineSendOptions, Poller, RawResponse, Request, RequestContent,
        Response, Url, UrlExt,
    },
    json, tracing, Result,
};

#[tracing::client]
pub struct NIClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl NIClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Resource read operation template.
    ///
    /// # Arguments
    ///
    /// * `api_version` - The API version to use for this operation.
    /// * `options` - Optional parameters for the request.
    #[tracing::function("N.I.getStatus")]
    pub async fn get_status(
        &self,
        api_version: &str,
        a: &str,
        options: Option<NIClientGetStatusOptions<'_>>,
    ) -> Result<Response<X>> {
        if a.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter a cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/x/{a}");
        path = path.replace("{a}", a);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Long-running operation.
    ///
    /// # Arguments
    ///
    /// * `api_version` - The API version to use for this operation.
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`NIClientIncorrectCustomOpRefOperationStatusHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use lro::models::{NIClientIncorrectCustomOpRefOperationStatus, NIClientIncorrectCustomOpRefOperationStatusHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<NIClientIncorrectCustomOpRefOperationStatus> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(operation_location) = response.operation_location()? {
    ///         println!("operation-location: {:?}", operation_location);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`operation_location`()](crate::generated::models::NIClientIncorrectCustomOpRefOperationStatusHeaders::operation_location) - operation-location
    ///
    /// [`NIClientIncorrectCustomOpRefOperationStatusHeaders`]: crate::generated::models::NIClientIncorrectCustomOpRefOperationStatusHeaders
    #[tracing::function("N.I.incorrectCustomOpRef")]
    pub fn incorrect_custom_op_ref(
        &self,
        api_version: &str,
        a: &str,
        options: Option<NIClientIncorrectCustomOpRefOptions<'_>>,
    ) -> Result<Poller<NIClientIncorrectCustomOpRefOperationStatus>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/lro/incorrect-custom-op-ref/{a}");
        path = path.replace("{a}", a);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", api_version);
        query_builder.build();
        Ok(Poller::new(
            move |poller_state: PollerState<Url>, poller_options| {
                let (mut request, next_link) = match poller_state {
                    PollerState::More(next_link) => {
                        let request = Request::new(next_link.clone(), Method::Get);
                        (request, next_link)
                    }
                    PollerState::Initial => {
                        let request = Request::new(url.clone(), Method::Post);
                        (request, url.clone())
                    }
                };
                let ctx = poller_options.context.clone();
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &ctx,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200, 202],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let next_link = match headers
                        .get_optional_string(&HeaderName::from_static("operation-location"))
                    {
                        Some(operation_location) => Url::parse(&operation_location)?,
                        None => next_link,
                    };
                    let retry_after = get_retry_after(
                        &headers,
                        &[X_MS_RETRY_AFTER_MS, RETRY_AFTER_MS, RETRY_AFTER],
                        &poller_options,
                    );
                    let res: NIClientIncorrectCustomOpRefOperationStatus = json::from_json(&body)?;
                    let mut final_rsp: Option<RawResponse> = None;
                    if res.status() == PollerStatus::Succeeded {
                        final_rsp = Some(RawResponse::from_bytes(
                            status,
                            headers.clone(),
                            body.clone(),
                        ));
                    }
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.status() {
                        PollerStatus::InProgress => PollerResult::InProgress {
                            response: rsp,
                            retry_after,
                            continuation_token: next_link,
                        },
                        PollerStatus::Succeeded => PollerResult::Succeeded {
                            response: rsp,
                            target: Box::new(move || {
                                Box::pin(async move {
                                    Ok(final_rsp
                                        .ok_or_else(|| {
                                            Error::new(ErrorKind::Other, "missing final response")
                                        })?
                                        .into())
                                })
                            }),
                        },
                        _ => PollerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }

    /// Long-running operation.
    ///
    /// # Arguments
    ///
    /// * `api_version` - The API version to use for this operation.
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`NIClientPartialBodyOperationStatusHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use lro::models::{NIClientPartialBodyOperationStatus, NIClientPartialBodyOperationStatusHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<NIClientPartialBodyOperationStatus> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(operation_location) = response.operation_location()? {
    ///         println!("operation-location: {:?}", operation_location);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`operation_location`()](crate::generated::models::NIClientPartialBodyOperationStatusHeaders::operation_location) - operation-location
    ///
    /// [`NIClientPartialBodyOperationStatusHeaders`]: crate::generated::models::NIClientPartialBodyOperationStatusHeaders
    #[tracing::function("N.I.partialBody")]
    pub fn partial_body(
        &self,
        api_version: &str,
        options: Option<NIClientPartialBodyOptions<'_>>,
    ) -> Result<Poller<NIClientPartialBodyOperationStatus>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut url = self.endpoint.clone();
        url.append_path("/lro/partial-body");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", api_version);
        query_builder.build();
        Ok(Poller::new(
            move |poller_state: PollerState<Url>, poller_options| {
                let (mut request, next_link) = match poller_state {
                    PollerState::More(next_link) => {
                        let mut request = Request::new(next_link.clone(), Method::Get);
                        request.insert_header("content-type", "application/json");
                        (request, next_link)
                    }
                    PollerState::Initial => {
                        let mut request = Request::new(url.clone(), Method::Post);
                        request.insert_header("content-type", "application/json");
                        let body: Result<RequestContent<PartialBodyRequest>> = PartialBodyRequest {
                            b: options.b.clone(),
                        }
                        .try_into();
                        if let Ok(body) = body {
                            request.set_body(body);
                        }
                        (request, url.clone())
                    }
                };
                let ctx = poller_options.context.clone();
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &ctx,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200, 202],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let next_link = match headers
                        .get_optional_string(&HeaderName::from_static("operation-location"))
                    {
                        Some(operation_location) => Url::parse(&operation_location)?,
                        None => next_link,
                    };
                    let retry_after = get_retry_after(
                        &headers,
                        &[X_MS_RETRY_AFTER_MS, RETRY_AFTER_MS, RETRY_AFTER],
                        &poller_options,
                    );
                    let res: NIClientPartialBodyOperationStatus = json::from_json(&body)?;
                    let mut final_rsp: Option<RawResponse> = None;
                    if res.status() == PollerStatus::Succeeded {
                        final_rsp = Some(RawResponse::from_bytes(
                            status,
                            headers.clone(),
                            body.clone(),
                        ));
                    }
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.status() {
                        PollerStatus::InProgress => PollerResult::InProgress {
                            response: rsp,
                            retry_after,
                            continuation_token: next_link,
                        },
                        PollerStatus::Succeeded => PollerResult::Succeeded {
                            response: rsp,
                            target: Box::new(move || {
                                Box::pin(async move {
                                    Ok(final_rsp
                                        .ok_or_else(|| {
                                            Error::new(ErrorKind::Other, "missing final response")
                                        })?
                                        .into())
                                })
                            }),
                        },
                        _ => PollerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }
}
