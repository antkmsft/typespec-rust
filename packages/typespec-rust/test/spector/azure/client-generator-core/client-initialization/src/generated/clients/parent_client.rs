// Copyright (c) Microsoft Corporation. All rights reserved.
//
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::clients::ParentChildClient;
use azure_core::{
    fmt::SafeDebug,
    http::{ClientOptions, Pipeline, Url},
    tracing, Result,
};

#[tracing::client]
pub struct ParentClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

/// Options used when creating a [`ParentClient`](ParentClient)
#[derive(Clone, Default, SafeDebug)]
pub struct ParentClientOptions {
    /// Allows customization of the client.
    pub client_options: ClientOptions,
}

impl ParentClient {
    /// Creates a new ParentClient requiring no authentication.
    ///
    /// # Arguments
    ///
    /// * `endpoint` - Service host
    /// * `options` - Optional configuration for the client.
    #[tracing::new("_Specs_.Azure.ClientGeneratorCore.ClientInitialization.ParentClient")]
    pub fn with_no_credential(
        endpoint: &str,
        options: Option<ParentClientOptions>,
    ) -> Result<Self> {
        let options = options.unwrap_or_default();
        let endpoint = Url::parse(endpoint)?;
        if !endpoint.scheme().starts_with("http") {
            return Err(azure_core::Error::message(
                azure_core::error::ErrorKind::Other,
                format!("{endpoint} must use http(s)"),
            ));
        }
        Ok(Self {
            endpoint,
            pipeline: Pipeline::new(
                option_env!("CARGO_PKG_NAME"),
                option_env!("CARGO_PKG_VERSION"),
                options.client_options,
                Vec::default(),
                Vec::default(),
                None,
            ),
        })
    }

    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Returns a new instance of ParentChildClient.
    ///
    /// # Arguments
    ///
    /// * `blob_name` - The name of the blob. This parameter is used as a path parameter in all operations.
    #[tracing::subclient]
    pub fn get_parent_child_client(&self, blob_name: String) -> ParentChildClient {
        ParentChildClient {
            blob_name,
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
        }
    }
}
