// Copyright (c) Microsoft Corporation. All rights reserved.
//
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::models::{
    RoutesPathParametersPathExpansionExplodeClientArrayOptions,
    RoutesPathParametersPathExpansionExplodeClientPrimitiveOptions,
    RoutesPathParametersPathExpansionExplodeClientRecordOptions,
};
use azure_core::{
    error::CheckSuccessOptions,
    http::{Method, NoFormat, Pipeline, PipelineSendOptions, Request, Response, Url},
    tracing, Result,
};
use std::collections::HashMap;

#[tracing::client]
pub struct RoutesPathParametersPathExpansionExplodeClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl RoutesPathParametersPathExpansionExplodeClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Routes.PathParameters.PathExpansion.Explode.array")]
    pub async fn array(
        &self,
        param: &[&str],
        options: Option<RoutesPathParametersPathExpansionExplodeClientArrayOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("routes/path/path/explode/array{param}");
        path = path.replace("{param}", &format!("/{}", param.join("/")));
        url = url.join(&path)?;
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Routes.PathParameters.PathExpansion.Explode.primitive")]
    pub async fn primitive(
        &self,
        param: &str,
        options: Option<RoutesPathParametersPathExpansionExplodeClientPrimitiveOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        if param.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter param cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("routes/path/path/explode/primitive{param}");
        path = path.replace("{param}", &format!("/{param}"));
        url = url.join(&path)?;
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Routes.PathParameters.PathExpansion.Explode.record")]
    pub async fn record(
        &self,
        param: &HashMap<String, i32>,
        options: Option<RoutesPathParametersPathExpansionExplodeClientRecordOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("routes/path/path/explode/record{param}");
        {
            let mut param_vec = param.iter().collect::<Vec<_>>();
            param_vec.sort_by_key(|p| p.0);
            path = path.replace(
                "{param}",
                &format!(
                    "/{}",
                    param_vec
                        .iter()
                        .map(|(k, v)| format!("{k}={v}"))
                        .collect::<Vec<_>>()
                        .join("/")
                ),
            );
        }
        url = url.join(&path)?;
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
